import styled from 'styled-components';

const HomeWrapper = styled.div`
    height: 100vh;
`;

const MainCurrencyWrapper = styled.div`
    display: flex;
    max-width: 1024px;
    margin: auto;
`;

export {
    HomeWrapper,
    MainCurrencyWrapper
};